---
skill_name: PPT通用内页组件规范
skill_type: page_spec
description: 目录页、图文页、要点列表、数据模块、多栏布局通用排版规则
trigger_keywords: ["PPT内页","正文页","图文排版","要点模块","目录页"]
compatible_skill: ["PPT封面首页规范","PPT封底规范","snippet碎片组件库","整套主题包"]
conflict_skill: []
usage_note: 定义页面骨架，主题包只会替换色彩与装饰，不改动页面结构
---
【正文写入各类内页版式规范】
---
skill_name: PPT通用内页组件规范
skill_type: page_spec
description: PPT中间正文页面通用骨架，包含章节分隔页、目录页、图文页、多栏卡片、数据页、要点列表；只定义布局结构，色彩、装饰交由theme主题包。
trigger_keywords: ["PPT内页","正文页","图文排版","要点模块","目录页","章节页","数据页"]
compatible_skill: ["PPT封面首页规范","PPT封底规范","PPT碎片组件库","整套主题包"]
conflict_skill: []
usage_note: 本文件只管理页面结构、排版、边距、组件布局；颜色、特殊装饰元素由theme主题包覆盖；禁止同时加载多个theme包。画布16:9。
---
# PPT通用内页组件规范
## 全局页面基础约束
16:9画布；页面四边统一安全边距：上下60px，左右70px，内容不要顶到画布边缘。

### 字体基础（可被theme覆盖）
- 章节标题：32‑36px
- 模块小标题：24‑28px
- 正文：16‑20px
- 注释小字：12‑14px

## 内置可复用页面骨架
### 1.目录页骨架
- 布局：双列卡片布局；序号+模块标题+简短说明。
- 组件：4个以内目录项最佳；超过4项分两页。
- theme主题包可以替换序号图标（例如葫芦图标替换数字序号）。

### 2.章节分隔页骨架
- 大章节编号 + 章节标题；少量辅助说明文字。
- 留白要多，不要塞入正文内容，仅做章节过渡。

### 3.左右分栏图文页骨架
- 左文字、右图片 / 左图片、右文字两种模式。
- 图片和文字区域宽度比例推荐 1:1 或 2:3。
- 图片禁止遮挡文本块。

### 4.多栏卡片模块（2/3/4栏）
- 等宽网格排布；每栏：标题 + 简短描述，可配小图标/小图。
- 卡片之间间距 ≥20px；不要挤在一起。适合价值、痛点、角色拆解。

### 5.要点列表页骨架
- 标题 + 多条要点；每条要点：符号前缀 + 标题 + 简短解释。
- 不要大段长文本，段落拆分，行高宽松。

### 6.数据指标页骨架
- 指标卡片：数字高亮 + 标签说明。
- 表格/图表：图表底色透明；文字标签清晰；图表放置在安全边距内。

### 7.闭环流程/时间线骨架
- 横向/纵向时间线；节点+连接线；每个节点包含标题+简短说明。
> theme主题包可以替换节点样式，例如葫芦图标做时间节点。

## 排版硬性规则
1. 一页聚焦1个核心信息，拒绝一页塞多个无关主题。
2. 段落行高 1.4‑1.6，便于投影阅读。
3. 卡片容器统一圆角，统一内边距。
4. 背景图只做底层，不能干扰正文阅读，必要时叠加低透明度蒙版。

## 和主题包联动说明
> 本规范只管布局结构；边框颜色、背景色、IP装饰、特殊字体全部由theme主题包接管。

